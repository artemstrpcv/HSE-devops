# Lab-1
* sudo vagrant up
wait till it's done
* curl --insecure --location --request GET 'https://127.0.0.1/' \--header 'Host: dynamic.devops'
* curl --insecure --location --request GET 'https://127.0.0.1/' \--header 'Host: static.devops'